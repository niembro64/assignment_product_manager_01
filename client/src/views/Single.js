import React from "react";
import { useEffect, useState, createElement } from "react";


const Single = (props) => {
  const [one, setOne] = useState({});

  return (
    <div>
      <h1>
        <marquee>home page here!</marquee>
      </h1>
    </div>
  );
};

export default Single;
